# ZineInsights — Dépendances Python (scripts/validation, tools/audit-interne)
# Installation : python -m venv .venv && .venv\Scripts\activate && pip install -r requirements.txt

pandas>=2.0
numpy>=1.24
openpyxl>=3.1
